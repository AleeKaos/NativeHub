@file:Suppress("DEPRECATION", "unused")

package com.example.nativehub

/**
 * ⚠️ ARQUIVO DEPRECATED
 * 
 * Este arquivo foi consolidado em:
 * - TabRegistry.kt (novo TabRegistry otimizado + WebViewConfigManager)
 * - GenericTabActivity.kt (nova activity genérica que substitui os 19 TabActivity duplicados)
 * - AndroidManifest.xml (atualizado para usar GenericTabActivity)
 * 
 * Não use as classes abaixo. Elas estão aqui apenas para compatibilidade transitória.
 * Remover este arquivo em versões futuras.
 * 
 * Antes do novo padrão:
 *   - 20 TabActivities duplicadas
 *   - Código repetido
 *   
 * Depois do novo padrão:
 *   - 1 GenericTabActivity reutilizável
 *   - 1 TabRegistry centralizado
 *   - WebViewConfigManager com configurações compartilhadas
 */

// As classes abaixo foram movidas para outros arquivos
// Mantidas aqui apenas para evitar quebra de compilação imediata
